package Stubs;

import DataAPI.ProductData;
import Domain.Category;
import Domain.Product;

public class ProductStub extends Product {
    public ProductStub(ProductData productData, Category category) {
        super(productData, category);
    }


}
